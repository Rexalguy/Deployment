fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
# Allow any 2.x torch release >=2.7 but avoid forcing a downgrade if a newer 2.x is present
torch>=2.7.0,<3.0
# Let pip pick a torchvision compatible with the installed torch 2.x series
torchvision>=0.24.0,<1.0
opencv-python==4.8.1.78
numpy==1.26.4
decord==0.6.0
pillow==10.0.1
python-dotenv==1.0.0